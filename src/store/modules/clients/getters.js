const getters = {
  yearEnd(state) {
    return state.yearEnd;
  },
  yearEndSingle(state) {
    return state.yearEndSingle;
  },
  salesOverview(state) {
    return state.salesOverview;
  },
};

export default getters;
